{
  "name": "skelegit-app",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/skelegit-app/src",
  "tags": ["type:app"],
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "apps/skelegit-app/dist"
      },
      "configurations": {
        "development": {
          "mode": "development"
        },
        "production": {
          "mode": "production"
        }
      }
    },
    "serve": {
      "executor": "@nx/vite:dev-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "skelegit-app:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "skelegit-app:build:development",
          "hmr": true
        },
        "production": {
          "buildTarget": "skelegit-app:build:production",
          "hmr": false
        }
      }
    },
    "preview": {
      "executor": "@nx/vite:preview-server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "skelegit-app:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "skelegit-app:build:development"
        },
        "production": {
          "buildTarget": "skelegit-app:build:production"
        }
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "passWithNoTests": true,
        "reportsDirectory": "../../coverage/apps/skelegit-app"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/skelegit-app/**/*.{ts,tsx,js,jsx}"]
      }
    }
  }
}
